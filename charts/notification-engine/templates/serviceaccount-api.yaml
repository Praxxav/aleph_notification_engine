apiVersion: v1
kind: ServiceAccount
metadata:
  name: notification-api
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: api
  annotations:
    azure.workload.identity/client-id: {{ .Values.api.managedIdentityClientId | quote }}
