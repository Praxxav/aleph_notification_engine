fastapi==0.115.0
uvicorn[standard]==0.31.0
asyncpg==0.29.0
pydantic==2.9.0
httpx==0.27.2

# Azure SDKs — used by AzureStorageQueueClient and AzureServiceBusClient.
# Required at runtime when QUEUE_BACKEND is 'storage' or 'servicebus'.
azure-identity==1.18.0
azure-storage-queue==12.11.0
azure-servicebus==7.12.3
